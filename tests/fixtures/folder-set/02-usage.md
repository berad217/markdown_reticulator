# Usage

The second file. Includes a fenced code block to confirm syntax highlighting
survives a combine.

## Example

```python
def reticulate(splines):
    return [s.smooth() for s in splines]
```

## Notes

Heading text intentionally overlaps with the appendix (see "Notes" there) to
exercise the TOC slug de-duplication across combined files.
