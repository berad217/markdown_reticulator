# Introduction

This is the **first** file in a multi-file set. Drop the whole `folder-set`
directory in to load all three at once.

## Goals

- Verify multi-file loading
- Verify the sidebar list
- Verify combine ordering

> The files are numbered `01-`, `02-`, `03-` so alphabetical load order is
> obvious and predictable.
